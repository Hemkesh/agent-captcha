# Agent-CAPTCHA Environment Configuration

# Port to run the server on (default: 3000)
PORT=3000

# Base URL for generating challenge URLs (auto-detected if not set)
# Set this when running behind a reverse proxy
# BASE_URL=https://captcha.yourdomain.com

# Admin password for protecting the dashboard
# If not set, dashboard is publicly accessible (not recommended for production)
# ADMIN_PASSWORD=your-secure-password-here
