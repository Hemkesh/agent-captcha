services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - BASE_URL=http://localhost:3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
